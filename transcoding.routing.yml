
transcoding.transcoding_config_form:
  path: '/admin/config/transcoding/config'
  defaults:
    _form: '\Drupal\transcoding\Form\ConfigForm'
    _title: 'ConfigForm'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE

